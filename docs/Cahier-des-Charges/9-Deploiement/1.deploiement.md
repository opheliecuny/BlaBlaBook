# Déploiement de BlaBlaBook en Production

Guide complet et détaillé pour déployer BlaBlaBook sur **Neon (PostgreSQL)** et **Render (API Express + Frontend Next.js)**.

> **📅 Dernière mise à jour** : 2026-03-25
> **✅ Statut** : Guide testé et validé en production

---

## Architecture de Déploiement

```plaintext
┌─────────────────┐
│   Utilisateur   │
└────────┬────────┘
         │
         │ HTTPS
         ▼
┌─────────────────────────────────────┐
│  Frontend (Render)                  │
│  - Next.js 16                       │
│  - App Router                       │
│  URL: blablabook-front.onrender.com │
└──────────┬──────────────────────────┘
           │
           │ API REST (HTTPS)
           ▼
┌─────────────────────────────────────┐
│  Backend (Render)                   │
│  - Express 5.2.1                    │
│  - Node.js 22                       │
│  URL: blablabook-api.onrender.com   │
└──────────┬──────────────────────────┘
           │
           │ PostgreSQL (SSL)
           ▼
┌─────────────────────────────────────┐
│  Database (Neon)                    │
│  - PostgreSQL 17                    │
│  - Serverless                       │
└─────────────────────────────────────┘
```

---

## Prérequis

### Comptes à créer

- [x] [Neon](https://neon.tech/) - Base de données PostgreSQL
- [x] [Render](https://render.com/) - Hébergement backend et frontend

### Vérifications locales

Avant de déployer, vérifier que le projet fonctionne localement :

```bash
# Backend
cd blablabook/backend
npm run lint          # Pas d'erreurs ESLint
npm test             # 73 tests passent
npm run build        # Compilation TypeScript OK

# Frontend
cd blablabook/frontend
npm run lint         # Pas d'erreurs ESLint
npm run build        # Build Next.js OK
```

---

## 🗄️ Etape 1 : Déployer la Base de Données (Neon)

### 1.1. Créer le Projet Neon

1. Se connecter sur [Neon](https://neon.tech/) avec GitHub
2. Cliquer sur **"Create Project"**
3. Configuration :
   - **Project name** : `blablabook-db`
   - **Region** : `AWS Europe Central 1 (Frankfurt)` ou `AWS Europe West 2 (London)`
   - **PostgreSQL version** : `17` (pour correspondre au projet)
   - **Compute size** : `Shared` (gratuit)

4. Attendre la création (quelques secondes)

### 1.2. Récupérer les Informations de Connexion

1. Aller dans **Dashboard → Connection Details**
2. Copier la **Connection string** complète :

   ```plaintext
   postgresql://[user]:[password]@[host]/[database]?sslmode=require
   ```

   Exemple :

   ```plaintext
   postgresql://neondb_owner:AbCdEf123456@ep-cool-breeze-12345678.eu-central-1.aws.neon.tech/blablabook?sslmode=require
   ```

3. **⚠️ IMPORTANT** : Sauvegarder cette URL en lieu sûr

### 1.3. Tester la Connexion Localement

1. Créer un fichier `.env.production` dans `blablabook/backend` :

   ```env
   DATABASE_URL="postgresql://[copier URL Neon ici]"
   PORT=3000
   JWT_SECRET="[générer une clé secrète forte]"
   JWT_EXPIRES_IN="7d"
   ALLOWED_ORIGINS="https://blablabook.vercel.app"
   NODE_ENV="production"
   ```

2. Appliquer les migrations Prisma :

   ```bash
   cd blablabook/backend
   DATABASE_URL="[URL Neon]" npx prisma migrate deploy
   ```

3. Vérifier que les tables sont créées :

   ```bash
   DATABASE_URL="[URL Neon]" npx prisma studio
   ```

   Ouvrir <http://localhost:5555> et vérifier les tables `user`, `book`, `library_item`, `refresh_token`

### 1.4. (Optionnel) Seed la Base de Production

```bash
DATABASE_URL="[URL Neon]" npm run db:seed
```

---

## 🚀 Etape 2 : Déployer le Backend (Render)

> **ℹ️ Note importante** : Ce guide utilise l'approche **sans Dockerfile** (Runtime Node natif) car le projet utilise des modules ES (ESM) avec TypeScript, et `tsx` gère mieux cette configuration que `tsc`. Cette approche est plus simple et plus fiable pour ce projet.

### 2.1. Déployer sur Render

1. **Se connecter** sur [Render](https://render.com/) avec GitHub

2. **Créer un nouveau Web Service** :
   - Cliquer sur **"New +" → "Web Service"**
   - Connecter votre dépôt GitHub
   - Sélectionner la branche `main`

3. **Configuration du service** :

   | Paramètre          | Valeur                                          |
   | ------------------ | ----------------------------------------------- |
   | **Name**           | `blablabook-api`                                |
   | **Language**       | `Node`                                          |
   | **Branch**         | `main`                                          |
   | **Region**         | `Frankfurt (EU Central)`                        |
   | **Root Directory** | `blablabook/backend`                            |
   | **Build Command**  | `npm install && npx prisma generate`            |
   | **Start Command**  | `npx prisma migrate deploy && npx tsx index.ts` |
   | **Instance Type**  | `Free`                                          |

4. **Variables d'environnement** :

   Aller dans **Environment → Environment Variables** et ajouter :

   | Key               | Value                                   | Notes                                  |
   | ----------------- | --------------------------------------- | -------------------------------------- |
   | `DATABASE_URL`    | `[URL Neon complète]`                   | Copier depuis Neon                     |
   | `JWT_SECRET`      | `[clé secrète 32+ caractères]`          | Générer avec `openssl rand -base64 32` |
   | `JWT_EXPIRES_IN`  | `7d`                                    | Durée de validité des tokens           |
   | `PORT`            | `3000`                                  | Port de l'application                  |
   | `ALLOWED_ORIGINS` | `https://blablabook-front.onrender.com` | À adapter selon URL frontend Render    |
   | `NODE_ENV`        | `production`                            | Mode production                        |

5. **Créer le service** :
   - Cliquer sur **"Create Web Service"**
   - Attendre la fin du build (5-10 minutes)

6. **Récupérer l'URL du backend** :
   - Format : `https://blablabook-api.onrender.com`
   - **Copier cette URL** (nécessaire pour le frontend)

### 2.2. Tester l'API Déployée

1. **Vérifier les logs** :
   - Aller dans **Logs** sur Render
   - Chercher `🚀 Server started at http://localhost:3000`
   - Vérifier que les migrations Prisma se sont bien appliquées

2. **Tester avec curl** :

   ```bash
   # Health check (si implémenté)
   curl https://blablabook-api.onrender.com/health-check

   # Test inscription
   curl -X POST https://blablabook-api.onrender.com/auth/register \
     -H "Content-Type: application/json" \
     -d '{
       "email": "test@example.com",
       "password": "Password123",
       "confirmPassword": "Password123",
       "username": "testuser"
     }'
   ```

3. **Vérifier la base de données** :

   ```bash
   # Ouvrir Prisma Studio sur la DB Neon
   DATABASE_URL="[URL Neon]" npx prisma studio
   ```

   Vérifier que l'utilisateur de test a bien été créé

---

## 🌐 Etape 3 : Déployer le Frontend (Render)

### 3.1. Préparer le Frontend

#### Vérifier les Variables d'Environnement

Dans `blablabook/frontend/.env.local` (pour tests locaux) :

```env
NEXT_PUBLIC_API_URL=http://localhost:3000
API_URL=http://localhost:3000
```

⚠️ **NE PAS COMMIT** ce fichier (déjà dans `.gitignore`)

#### Vérifier `next.config.ts`

S'assurer que la configuration Next.js est correcte :

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuration pour les images distantes
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "covers.openlibrary.org",
        pathname: "/b/**",
      },
    ],
  },
};

export default nextConfig;
```

> **ℹ️ Note** : Pas de `output: "standalone"` - on utilise le mode standard avec `next start`

### 3.2. Déployer sur Render

1. **Se connecter** sur [Render](https://render.com/) avec GitHub

2. **Créer un nouveau Web Service** :
   - Cliquer sur **"New +" → "Web Service"**
   - Connecter votre dépôt GitHub (si pas déjà fait)
   - Sélectionner la branche `main`

3. **Configuration du service** :
3. **Configuration du service** :

   | Paramètre          | Valeur                         |
   | ------------------ | ------------------------------ |
   | **Name**           | `blablabook-frontend`          |
   | **Language**       | `Node`                         |
   | **Branch**         | `main`                         |
   | **Region**         | `Frankfurt (EU Central)`       |
   | **Root Directory** | `blablabook/frontend`          |
   | **Build Command**  | `npm install && npm run build` |
   | **Start Command**  | `npm run start`                |
   | **Instance Type**  | `Free`                         |

4. **Variables d'Environnement** :

   Aller dans **Environment → Environment Variables** et ajouter :
   Aller dans **Environment → Environment Variables** et ajouter :

   | Key                   | Value                                 | Notes                               |
   | --------------------- | ------------------------------------- | ----------------------------------- |
   | `NEXT_PUBLIC_API_URL` | `https://blablabook-api.onrender.com` | Pour Client Components (navigateur) |
   | `API_URL`             | `https://blablabook-api.onrender.com` | Pour Server Components (SSR)        |
   | `NODE_ENV`            | `production`                          | Mode production                     |

   > **ℹ️ Pourquoi deux variables ?**
   > - `NEXT_PUBLIC_API_URL` : Utilisée par les composants côté client (navigateur)
   > - `API_URL` : Utilisée par les Server Components (rendu côté serveur) comme la page de recherche
   >
   > **Sans `API_URL`, la recherche de livres et la page d'accueil ne fonctionneront pas !**

5. **Créer le service** :
   - Cliquer sur **"Create Web Service"**
   - Attendre la fin du build (5-10 minutes)

6. **Récupérer l'URL du frontend** :
   - Format : `https://blablabook-frontend-xxxx.onrender.com`
   - Ou utiliser un nom personnalisé dans les settings
   - **Copier cette URL** (nécessaire pour la configuration CORS backend)

### 3.3. Configuration CORS Backend

Mettre à jour la variable `ALLOWED_ORIGINS` sur Render (backend) :

1. Aller sur Render → Service `blablabook-api` (backend)
2. **Environment → Environment Variables**
3. Modifier `ALLOWED_ORIGINS` avec l'URL du frontend :

   ```plaintext
   https://blablabook-frontend-xxxx.onrender.com
   ```

   Ou pour plusieurs origines (développement + production) :

   ```plaintext
   https://blablabook-frontend-xxxx.onrender.com,http://localhost:3001
   ```

4. **Sauvegarder** → Le service backend redémarre automatiquement (~30s)

### 3.4. Tester l'Application Complète

1. **Ouvrir l'URL Render** dans le navigateur
2. **Tester le flux complet** :
   - ✅ Inscription d'un nouvel utilisateur
   - ✅ Connexion
   - ✅ Recherche de livres (API OpenLibrary)
   - ✅ Ajout d'un livre à la bibliothèque
   - ✅ Changement de statut de lecture
   - ✅ Suppression d'un livre
   - ✅ Modification du profil
   - ✅ Déconnexion

3. **Vérifier la console navigateur (F12)** :
   - Pas d'erreurs CORS
   - Pas d'erreurs 401/403
   - Requêtes API passent bien

4. **Vérifier les logs Render** :
   - Frontend : Pas d'erreurs au démarrage
   - Backend : Requêtes API visibles dans les logs

---

## 🔄 Etape 4 : Automatiser le Déploiement (CI/CD)

### 4.1. Configuration Actuelle

Le projet a déjà un workflow CI dans `.github/workflows/ci.yml` avec 5 jobs :

- `lint-backend`
- `test-backend` (73 tests avec PostgreSQL)
- `build-backend`
- `lint-frontend`
- `build-frontend`

### 4.2. Ajouter le Déploiement Automatique

Créer `.github/workflows/deploy.yml` :

```yaml
name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permet déclenchement manuel

jobs:
  # Job 1 : Déployer Backend sur Render
  deploy-backend:
    name: Deploy Backend (Render)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Render Deploy Hook (Backend)
        run: |
          curl -X POST ${{ secrets.RENDER_BACKEND_DEPLOY_HOOK }}

  # Job 2 : Déployer Frontend sur Render
  deploy-frontend:
    name: Deploy Frontend (Render)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Render Deploy Hook (Frontend)
        run: |
          curl -X POST ${{ secrets.RENDER_FRONTEND_DEPLOY_HOOK }}
```

### 4.3. Configurer les Secrets GitHub

Depuis le dépôt GitHub du projet, aller dans **Settings → Secrets and variables → Actions** et ajouter les secrets suivants :

#### Pour le Backend

1. Créer un **Deploy Hook** sur Render :
   - Aller sur Render → Service `blablabook-api`
   - **Settings → Deploy Hook**
   - Copier l'URL générée

2. Ajouter le secret :
   - **Nom** : `RENDER_BACKEND_DEPLOY_HOOK`
   - **Value** : `[URL du deploy hook backend]`

#### Pour le Frontend

1. Créer un **Deploy Hook** sur Render :
   - Aller sur Render → Service `blablabook-frontend`
   - **Settings → Deploy Hook**
   - Copier l'URL générée

2. Ajouter le secret :
   - **Nom** : `RENDER_FRONTEND_DEPLOY_HOOK`
   - **Value** : `[URL du deploy hook frontend]`

### 4.4. Tester le Workflow

```bash
# Pousser sur main
git add .
git commit -m "feat: setup automatic deployment"
git push origin main

# Le workflow se déclenche automatiquement
# Vérifier dans GitHub → Actions
```

> **ℹ️ Note** : Les deux services Render se redéploient automatiquement à chaque push sur `main`

---

## 🎯 Etape 5 : Post-Déploiement

### 5.1. Monitoring et Logs

#### Neon (Base de Données)

- **Tableau de bord** : <https://console.neon.tech/>
- **Métriques** : Connexions actives, CPU, Mémoire
- **Alertes** : Configurer dans **Settings → Monitoring**

⚠️ **Free tier** : La base s'endort après 5 min d'inactivité (redémarre automatiquement)

#### Render (Backend)

- **Logs en temps réel** : Dashboard → Logs
- **Métriques** : CPU, Mémoire, Requêtes HTTP
- **Alertes** : Settings → Notifications

⚠️ **Free tier** : Service s'endort après 15 min d'inactivité (démarrage ~30s)

**Solution pour garder actif** :

- Utiliser [UptimeRobot](https://uptimerobot.com/) pour ping toutes les 5 min
- Endpoint à ping : `https://blablabook-api.onrender.com/health`

#### Render (Frontend)
#### Render (Frontend)

- **Logs en temps réel** : Dashboard → Logs
- **Métriques** : CPU, Mémoire, Requêtes HTTP
- **Alertes** : Settings → Notifications

⚠️ **Free tier** : Service s'endort après 15 min d'inactivité (démarrage ~30s)

**Solution pour garder actif** :

- Utiliser [UptimeRobot](https://uptimerobot.com/) pour ping toutes les 5 min
- Endpoint à ping : `https://blablabook-frontend-xxxx.onrender.com`

### 5.2. Sauvegardes Base de Données

#### Sauvegarde Manuelle

```bash
# Export complet de la base Neon
pg_dump "[URL Neon]" > backup-$(date +%Y%m%d).sql

# Compression
gzip backup-$(date +%Y%m%d).sql
```

#### Sauvegarde Automatique (GitHub Actions)

Créer `.github/workflows/backup.yml` :

```yaml
name: Database Backup

on:
  schedule:
    - cron: '0 2 * * *' # Tous les jours à 2h du matin
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Backup Database
        run: |
          pg_dump "${{ secrets.DATABASE_URL }}" > backup.sql

      - name: Upload to GitHub Releases
        uses: actions/upload-artifact@v4
        with:
          name: database-backup-${{ github.run_number }}
          path: backup.sql
          retention-days: 30
```

Ajouter le secret `DATABASE_URL` (URL Neon) dans GitHub Secrets.

### 5.3. Domaine Personnalisé (Optionnel)

#### Pour le Frontend (Render)

1. Acheter un domaine (ex: [Namecheap](https://www.namecheap.com/), [Google Domains](https://domains.google/))

2. Sur Render :
   - Aller sur le service `blablabook-frontend`
   - **Settings → Custom Domain**
   - Ajouter `blablabook.com` (ou `www.blablabook.com`)

3. Configuration DNS :

   ```plaintext
   Type: CNAME
   Name: www
   Value: blablabook-frontend-xxxx.onrender.com
   ```

   Ou pour le domaine racine :

   ```plaintext
   Type: A
   Name: @
   Value: [Adresses IP fournies par Render]
   ```

#### Pour le Backend (Render)

1. Sur Render :
   - **Settings → Custom Domain**
   - Ajouter `api.blablabook.com`

2. Configuration DNS :

   ```plaintext
   Type: CNAME
   Name: api
   Value: blablabook-api.onrender.com
   ```

3. Mettre à jour `ALLOWED_ORIGINS` sur Render (backend) :

   ```plaintext
   https://blablabook.com,https://www.blablabook.com
   ```

4. Mettre à jour les variables d'environnement sur Render (frontend) :

   - `NEXT_PUBLIC_API_URL` : `https://api.blablabook.com`
   - `API_URL` : `https://api.blablabook.com`

---

## ⚠️ Points d'Attention

### Limitations Free Tier

| Service    | Limitation                    | Impact                                | Solution                              |
| ---------- | ----------------------------- | ------------------------------------- | ------------------------------------- |
| **Neon**   | Base s'endort après 5 min     | Première requête lente (~2s)          | UptimeRobot ou passer au plan Pro     |
| **Render** | Service s'endort après 15 min | Démarrage à froid ~30s                | UptimeRobot ou passer au plan Starter |
| **Render** | 750h/mois gratuit             | Service peut s'arrêter en fin de mois | Surveiller l'usage                    |

### Sécurité

- ✅ **HTTPS** : Activé par défaut sur Vercel et Render
- ✅ **Cookies httpOnly** : Déjà configuré dans le code
- ✅ **CORS** : Configuré via `ALLOWED_ORIGINS`
- ✅ **Helmet** : Déjà activé dans Express
- ⚠️ **JWT_SECRET** : Utiliser une clé forte (32+ caractères aléatoires)

Générer une clé secrète forte :

```bash
openssl rand -base64 32
```

### Performance

#### Backend (Render)

- `tsx` compile TypeScript à la volée (JIT) avec un overhead minimal
- Prisma Client est généré pendant le build pour optimiser les performances
- Les dépendances sont mises en cache par Render entre les builds

#### Frontend (Render)
#### Frontend (Render)

- Images OpenLibrary : déjà optimisées via `next/image`
- Next.js en mode production : bundle optimisé automatiquement
- Mise en cache des assets statiques

---

## 📝 Checklist de Déploiement

### Avant le Déploiement

- [ ] Tests locaux passent (73 tests backend)
- [ ] Lint sans erreurs (backend + frontend)
- [ ] Build réussi (backend + frontend)
- [ ] Variables d'environnement documentées
- [ ] Secrets générés (JWT_SECRET)

### Déploiement Base de Données

- [ ] Compte Neon créé
- [ ] Projet Neon créé (région EU)
- [ ] URL de connexion sauvegardée
- [ ] Migrations Prisma appliquées
- [ ] Tables vérifiées dans Prisma Studio

### Déploiement Backend

- [x] Service Render créé
- [x] Configuration Build/Start commands
- [x] Variables d'environnement configurées
- [x] Build réussi sur Render
- [ ] Logs vérifiés (pas d'erreurs)
- [ ] Test API avec curl

### Déploiement Frontend

- [ ] Service Render créé
- [ ] `NEXT_PUBLIC_API_URL` configurée
- [ ] `API_URL` configurée (Server Components)
- [ ] Build réussi sur Render
- [ ] Application accessible
- [ ] CORS configuré (backend)

### Post-Déploiement

- [ ] CORS configuré (backend)
- [ ] Flux complet testé (register → login → CRUD)
- [ ] CI/CD configuré (deploy.yml)
- [ ] Secrets GitHub ajoutés
- [ ] Monitoring activé
- [ ] Sauvegarde automatique configurée

---

## 🆘 Troubleshooting

### Erreur : "Database connection failed"

**Symptôme** : Backend ne démarre pas, logs Render montrent erreur Prisma

**Solutions** :

1. Vérifier `DATABASE_URL` dans Render (copié correctement depuis Neon)
2. Vérifier que `?sslmode=require` est présent
3. Tester la connexion localement :

   ```bash
   DATABASE_URL="[URL]" npx prisma migrate deploy
   ```

### Erreur CORS : "Access-Control-Allow-Origin"

**Symptôme** : Frontend ne peut pas appeler l'API, erreur dans console navigateur

**Solutions** :

1. Vérifier `ALLOWED_ORIGINS` sur Render (backend) contient l'URL exacte du frontend Render
2. Vérifier que l'URL est exacte (pas de `/` à la fin)
3. Vérifier les logs Render (backend) pour voir les requêtes
4. Exemple correct :

   ```plaintext
   ALLOWED_ORIGINS=https://blablabook-frontend-xxxx.onrender.com
   ```

### Frontend affiche des erreurs 401

**Symptôme** : Impossible de se connecter, toutes les requêtes retournent 401

**Solutions** :

1. Vérifier que `JWT_SECRET` est identique à celui utilisé lors du register
2. Vérifier les cookies dans DevTools (F12 → Application → Cookies)
3. Vérifier que les cookies sont `httpOnly` et `secure`

### Service Render ne redémarre pas

**Symptôme** : Modifications poussées mais backend ne se met pas à jour

**Solutions** :

1. Vérifier que le commit est bien sur `main`
2. Vérifier le deploy hook dans GitHub Actions
3. Redémarrer manuellement : Render Dashboard → Manual Deploy

### La recherche de livres / page d'accueil ne fonctionne pas

**Symptôme** : La recherche de livres ne retourne aucun résultat, la page d'accueil n'affiche pas de livres, mais l'authentification fonctionne

**Cause** : Les Server Components (comme `/search/page.tsx`) utilisent `process.env.API_URL` qui n'est pas définie

**Solutions** :

1. Vérifier sur Render (frontend) que **DEUX** variables d'environnement sont configurées :

   ```plaintext
   NEXT_PUBLIC_API_URL=https://blablabook-api.onrender.com
   API_URL=https://blablabook-api.onrender.com
   ```

2. **Rebuilder le frontend** après avoir ajouté `API_URL` :
   - Render Dashboard → Service frontend → Manual Deploy → Clear build cache & deploy

3. Vérifier que les deux variables pointent vers la même URL backend

4. Tester en production :

   ```javascript
   // Dans la console du navigateur (test Client Component)
   fetch('https://blablabook-api.onrender.com/books/search?q=test&page=1')
     .then(r => r.json())
     .then(d => console.log('API fonctionne:', d.total, 'livres'))
   ```

> **💡 Rappel** : Next.js requiert deux variables car :
>
> - `NEXT_PUBLIC_API_URL` est baked dans le bundle JavaScript (accessible côté navigateur)
> - `API_URL` est uniquement disponible côté serveur (Server Components)

---

## 📚 Ressources

### Documentation Officielle

- [Neon Docs](https://neon.tech/docs)
- [Render Docs](https://render.com/docs)
- [Vercel Docs](https://vercel.com/docs)
- [Prisma Deployment](https://www.prisma.io/docs/guides/deployment)

### Outils Utiles

- [UptimeRobot](https://uptimerobot.com/) - Keep-alive gratuit
- [Namecheap](https://www.namecheap.com/) - Domaines pas chers
- [SSL Labs](https://www.ssllabs.com/ssltest/) - Test HTTPS
- [PageSpeed Insights](https://pagespeed.web.dev/) - Performance

---

**🎉 Félicitations ! BlaBlaBook est maintenant en production !**

Pour toute question ou problème, consulter la documentation ou ouvrir une issue sur GitHub.
