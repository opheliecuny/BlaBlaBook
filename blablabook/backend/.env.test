# Base de données de test (même serveur, base de données différente)
DATABASE_URL="postgresql://blablabook:blablabook@localhost:5434/testdb"

# Application
NODE_ENV=test
PORT=3001

# JWT
JWT_SECRET=test-secret-key-minimum-32-characters-long-for-testing
JWT_EXPIRES_IN=1h

# CORS
ALLOWED_ORIGINS="*"
